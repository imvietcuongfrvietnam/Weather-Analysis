pyspark>=3.3.0
pandas>=1.5.0
numpy>=1.23.0
minio
pyarrow>=10.0.0  # Required for Parquet support
kafka-python>=2.0.0  # Optional: For connection testing in connection_utils

# Machine Learning dependencies
tensorflow>=2.10.0  # For LSTM model
scikit-learn>=1.0.0  # For data preprocessing

# PostgreSQL dependencies
psycopg2-binary>=2.9.0  # PostgreSQL adapter for Python

# Note: Spark S3 connector cần thêm khi chạy Spark:
# spark-submit --packages org.apache.hadoop:hadoop-aws:3.3.1,com.amazonaws:aws-java-sdk-bundle:1.12.262,org.postgresql:postgresql:42.5.0
